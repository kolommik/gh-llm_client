class Model:
    def __init__(self, name, output_max_tokens):
        self.name = name
        self.output_max_tokens = output_max_tokens
        # self.context = context
        # self.output = output
        # self.last_update = last_update
        # self.price_input = price_input
        # self.price_output = price_output
